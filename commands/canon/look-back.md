---
description: Mine recent work for reusable canon assets
argument-hint: "[optional focus]"
allowed-tools: [Read, Glob, Grep, Bash, Skill]
---

# /canon:look-back

Route this slash command to the `look-back` skill.

The user invoked:

```text
/canon:look-back $ARGUMENTS
```

Use the `look-back` skill exactly as written. Treat `$ARGUMENTS` as an optional focus area, date range, repository area, or workflow name. Start with a shortlist and evidence before creating or editing anything.
