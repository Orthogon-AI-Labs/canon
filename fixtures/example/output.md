## Workflow

This is example output for the `templates/eval.yaml` starter eval.

It includes the phrase "specific output" and names a clear "stopping condition"
so the `contains` graders pass, and it avoids the phrase that would trip
`not_contains`.
