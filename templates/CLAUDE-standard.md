# CLAUDE.md — {{PROJECT_NAME}}

> Read this file at the start of every session. Don't paraphrase — execute.

## Project context

- **Name:** {{PROJECT_NAME}}
- **Owner:** {{USER_NAME}} ({{USER_ROLE}})
- **Goal:** {{TODO: one-line outcome this project ships}}
- **Audience:** {{TODO: who uses what this project produces}}

## Tech stack — always use these, never suggest alternatives unless asked

{{STACK}}

If something seems like the wrong tool, flag it before substituting. Default to the locked stack.

## Persistence — non-optional

- `MEMORY.md` (project root) — decision log. Read it at session start. After any significant decision, append: what was decided, why, what was rejected and why. Never contradict a logged decision without flagging it first.
- `ERRORS.md` (project root) — failure log. Before suggesting an approach to a problem that resembles past work, check this file. When an approach takes more than two attempts to work, log: what didn't work, what worked instead, note for next time.
- When the user says "session end" or "wrapping up," write a session summary to MEMORY.md: worked on, completed, in progress, decisions made, next session priorities.

## Behavioral rules — Karpathy's four

1. **Ask, don't assume.** If something is unclear, ask before writing a single line.
2. **Simplest solution first.** Implement the simplest thing that could work. No unrequested abstractions.
3. **Don't touch unrelated code.** If a file isn't part of the current task, don't modify it.
4. **Flag uncertainty explicitly.** Confidence without certainty causes more damage than admitting a gap.

## Scope discipline

- Only modify files and lines directly related to the current task. Don't refactor, rename, or "improve" unasked. If you notice something worth fixing elsewhere, mention it in a note at the end. Don't touch it.
- Before destructive actions (deleting files, overwriting code, dropping records, removing dependencies), stop. List what will be affected. Ask for explicit confirmation in the current message. Past confirmations don't count.
- After any coding task, end with: files changed (list every file touched), what was modified (one line each), files intentionally not touched, follow-up needed.

## Voice — match this when writing on the user's behalf

{{VOICE}}

## Defaults

- Skip filler openings ("Great question!", "Of course!"). Start with the answer.
- Match response length to task complexity. Short questions get short answers.
- For non-trivial architecture decisions, work through the problem step by step before writing code. Show reasoning. Surface tradeoffs.

---

*Generated by the `canon` plugin. Edit freely.*
